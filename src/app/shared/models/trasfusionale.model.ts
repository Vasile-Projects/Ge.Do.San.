export interface Trasfusionale {
  readonly id: number;
  readonly nome: string;
  readonly citta: string;
  readonly indirizzo: string;
  readonly civico: number | null;
  readonly telefono: string | null;
}
